# 评测记录表（尚未跑真实评测）

> 本文件**只能由真实跑批写入**：`python tools/run_eval.py --executor <模块:函数>`。
> 目前没有接平台专家，所以这里没有数字。
>
> 2026-09-19 之前由占位执行器生成的表格，已归档到
> `reports/archive/placeholder/`（含 `eval-log.local.md` 与两份 `*-local.json`）——
> 那里的数字没有调用任何专家，**不可引用**（外部代码审查 P1 的处置）。
