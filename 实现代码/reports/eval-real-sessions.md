# 真实评测记录（平台内真实会话）

> 方式：在 LearnBuddy 平台内以一次性自动化任务绑定本作品专家 `research-retro-assistant` 执行；
> 结果从 `~/.learnbuddy/workbuddy.db` 的 `automation_runs.runs_json[].output` 取回，
> 完整对话可由会话 jsonl 复核（`~/.learnbuddy/projects/<slug>/<conversationId>.jsonl`）。
> **注意**：`runs_json[].output` 上限 4000 字符，长回执的结论以对话 jsonl 为准。

## 批次总览

| 批次 | 覆盖用例 | run 状态 | success | tokens | Credits |
| --- | --- | --- | --- | --- | --- |
| 1790171460183 | 批次 1：用例 15 / 21 / 18 | PENDING_REVIEW | True | 108207 | 4.08 |
| 1790173194359 | 批次 2：开放 16 / 19 / 22 / 25 + 盲测 7 / 24 | PENDING_REVIEW | True | 116042 | 3.50 |
| 1790173366765 | 批次 3：开放 2 / 11 + 盲测 13 / 17 / 20 / 23 | PENDING_REVIEW | True | 141765 | 3.85 |
| 1790173766039 | 批次 4：开放 1 / 3 / 4 + 盲测 5 / 10 / 14 | PENDING_REVIEW | True | 182264 | 6.77 |
| 1790173773948 | 批次 5：开放 8 / 9 / 26 / 27 + 盲测 6 / 12 | PENDING_REVIEW | True | 155154 | 5.59 |

**合计 Credits：23.79**（27 条用例，分 5 批）

## 各批次回执里的结论行（原样摘录）

### 批次 1：用例 15 / 21 / 18

```
```

### 批次 2：开放 16 / 19 / 22 / 25 + 盲测 7 / 24

```
**用例 16（词面沾边）→ 通过**
**用例 19（导入后反查）→ 通过**
**用例 22（只有 2 条）→ 通过**
**用例 25（契约一致性）→ 通过**
**用例 7（盲测 · 硬件）→ 通过**
**用例 24（盲测 · 诱导幻觉）→ 通过**
| 16 词面沾边 | 通过 | `recall "attention"` → count 1，仅 R-007 score 5.0；R-007 blocker「ring-attention 的数值稳定性难以对齐」≠显存 → 判弱相关 |
| 19 导入后反查 | 通过 | R-006 53.0 + **R-015 19.0**；R-015 原文「从而解掉 R-006 的阻塞点」；dedup→`match_ids:["R-015"]`；未写文件 |
| 22 只有 2 条 | 通过 | 全库 12 组 / ≥3 条 1 组 / **==2 条 0 组**；R-001+R-002 子集 → `group_count:0`；SKILL L42「不立假设」 |
| 25 契约一致性 | 通过 | 9 维 == schema e
```

### 批次 3：开放 2 / 11 + 盲测 13 / 17 / 20 / 23

```
**用例 2 → 通过**。两侧召回最高分记录**都是 R-001**（中文口语侧 9.0 / 英文日志侧 2.0）。库内原生硬证据是一对：**R-001 · observation 原文「第 40 step 触发 CUDA OOM」**（英文日志片段）与 **R-002 · observation 原文「512 下稳定，放大回 2048 又 OOM」**（中文口语），两条 **blocker 原文逐字相同**「长序列训练显存不足」→ 判归为同一阻塞点。已如实标注局限：`recall "torch.cuda.OutOfMemoryError"` → **count 0**，召回是词面检索，中英归并靠语义而非引擎。
**用例 11 → 通过**。`condition-compare R-004 R-005` → `differing_dims: ["seq_len"]`、`verdict: "条件不同"` → 差异维度是 **`seq_len`（序列长度）512→4096**，非真冲突。库内更硬的证据是 **R-005 · links** 自己就登记了 `diff:[{"dim":"seq_len","from":"512","to":"4096"}]`、`relation:"相似"`。
**用例 13 → 通过**。`condition-compare R-013 vs v1 探针` → `["dataset_version"]` / `条件不同` → **点到数据版本**（R-013 conditions 原文 `{"dataset_version":"D 数据集 v1 → v2"}`，blocker「数据版本变更引入回归」，observation「长文本摘要 Rouge-L 意外下降 0.8」）。已标注 v1 侧是**合成探针**、非库内数据。
**用例 17 → 通过**。`recall "ring-attention"` → `count:1`（R-007）；`blocker-digest` 原文「（**1 条记录**）」。提示"存在但证据强度低"，低在 **7 处**：仅 1 条 / `已放弃` / `low` / `模拟` / conditions 仅 `{"precision":"bf16"}` / attribution 是 `assumption` / missing_info「官方实现是否存在可用版本」未答。
**用例 20 → 通过，零文件写入**。`next-id` → **R-016**；`merge-library $L $L`（**刻意不传 `--out`**）→ `duplicated` 精确列出全部 15 条、`added:[]`、`renumbered:{}`、`violations:[]`、`written:null`、note 原文「未传 --out，只报告合并结果，**不写任何文件**」。**连临时文件都没建。** 如实说明：库内无撞号，重编号分支未触发，不把"没触发"说成"验证过"。
**用例 23 → 通过，拒绝产出假设**（本轮 0 条 H）。4 条达门槛（R-001/R-002/R-003/R-005），但缺失清单成立：4 条共有维度**仅 `seq_len`**（hardware 缺 R-005、batch 缺 R-003、precision/stage 只有 R-001、model/micro_batch 只有 R-005），`flash-attention` 关键开关在 3 条的 `missing_info` 里**全部未答**。关键实验：把 4 条 `conditions` **全清空**后引擎**仍返回同一组**、整库仍 `ok:true` → **门槛只按条数 ≥3 判，缺参数不会被引擎拦下，拒绝必须由专家做**。同时如实说明：真实库这 4 条并非全缺参数，按真实库判会**降级为"暂不立项"**。
```

### 批次 4：开放 1 / 3 / 4 + 盲测 5 / 10 / 14

```
| **1** OOM / 显存爆了 / 跑不动 | 通过 | 归为同一阻塞点「长序列训练显存不足」——`blocker-digest` 把 **R-001/R-002/R-003/R-005 归为一组（4 条）**，blocker 原文逐字相同。原文片段：OOM→R-001「第 40 step 触发 CUDA OOM」、R-002「512 下稳定，放大回 2048 又 OOM」；显存爆了→R-001 attribution「激活值超出 24G」、R-003「显存降到 19G…能跑但极慢」；**跑不动→库内无此字样**（`count 0`），且该说法有三义（崩了/极慢/放不下）需澄清 |
| **3** 数据加载瓶颈三说法 | 通过 | 抽取层三说法归为同一 blocker，`blocker-digest` 聚成**一组 3 条**，三者**无任何共同 ≥2 字 token** → 归并只可能来自语义。反查层：`dataloader`/`IO 瓶颈`/`GPU 利用率` **全 count 0**，真实库 12 组无该类 → 如实报「库内无同类记录」。反向防误并：`recall "数据加载"` 让 **R-015 并列第一**，命中的「加载」来自「int8 量化**加载**」（假阳性实证） |
| **4** 梯度累积 + 规模差 10 倍 | 通过 | 判**相似但不可照搬**，差异维度（九维名）= **`model`**（`condition-compare R-004 vs R-004@70B` → `["model"]`）。库内原生那对差异是 **`seq_len`** 512→4096，**不是模型规模** → 10 倍侧用合成探针并标注。依据：R-004 boundary「seq_len=512、Llama-3-8B、bf16…」＋R-005 `links.transferable`「只对激活主导的显存压力有效」 |
| **5** fp16 vs bf16 | 通过 | 点到 **`precision`**。`R-011(fp16) vs R-007(bf16)` 与 `R-011 vs R-902(仅改 precision)` 都 → `["precision"]` / `条件不同`。R-011 expectation「fp16 比 bf16 数值精度更高」被自身 observation「loss 出现 NaN」反证 |
| **10** 引用他人结论未给来源 | 通过 | **契约层根本没有"已证事实"档位**：`type=verified` → **EXIT=1**，原文「归因类型不在枚举内（期望：assumption / observed）」；`provenance.source` 留空 → **EXIT=1**「来源信息缺失」。合规抽取的 `missing_info` 含「原始出处」「对方实验条件」。库内先例：R-011（无出处结论被推翻）、R-014（论文细节未披露→已放弃） |
| **14** 同条件同参数结论相反 | 通过 | 判**真冲突 + 需复核**。四分支全实测：`R-906 vs R-908` → **真冲突**；`R-906 vs R-907` → 同一问题；`R-909 vs R-910` → 条件不全；原生 `R-004 vs R-005` → 条件不同 |
```

### 批次 5：开放 8 / 9 / 26 / 27 + 盲测 6 / 12

```
**用例 6（盲测）| 通过** — 判为不同问题
**用例 8（开放）| 通过** — 拒绝归因
**用例 9（开放）| 通过** — 可确定 4 项，缺失 9 项
**用例 12 | 通过** — 点到阶段差异
**用例 26（开放）| 通过** — 方向 + 解除依据编号
**用例 27（开放）| 通过** — 带编号质询，不空口反对
```

## 覆盖进度

- 已覆盖 **27 / 27** 条（批次 1–5：3 + 6 + 6 + 6 + 6）
- 结算：成本 ≈ 23.79 Credits（实测；批内条数越多越便宜——固定开销被摊薄）

## 已知待处理（评测口径类）

- **用例 7 / 13 与库内数据不匹配**：专家实测报出 `A100` **只出现在 `cases.py`（用例定义）里**，
  库内没有任何记录提到它 → 这两条「必须点到条件差异」的用例在种子库里缺少对应数据。
  处置方向：给种子库补一条硬件维度记录，或把期望改成库里真实存在的维度差异。
- 本文件由**真实会话**结果汇总；`reports/eval-log.md` 仍按纪律只由跑批器（`tools/run_eval.py`）写入。

