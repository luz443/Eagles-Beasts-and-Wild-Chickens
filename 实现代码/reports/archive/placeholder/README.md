# 归档：占位产物（数字一律不可引用）

这里放的是**没有调用任何专家**的运行结果。保留它们不是留念，而是**纪律被执行的证据**：
`tools/run_eval.py` 在没有真实执行器时退出码为 1、且**绝不写** `reports/eval-log.md`，
把结果丢到 `.local.json` 这类边角文件里，就是为了让「占位数据冒充评测结果」这件事
在文件层面也做不到。

| 文件 | 说明 |
| --- | --- |
| `2026-09-19-placeholder-open.json` | 占位跑批（执行器交回「没有输出」），通过率 0 |
| `2026-09-19-local.json` | 同上，本地模式 |
| `eval-log.local.md` | 更早一版占位记录表；**已作废**，正式记录表见 `reports/eval-log.md` |

## 三条不可违反的口径

1. 这些文件里的任何数字**不得**出现在 PPT、视频、方案或对话里。
2. `reports/eval-log.md` **只能**由真实跑批写入（`--executor` 接了平台专家）。
3. 判据由 `rra.eval.runner.Judge` 从原始输出与库算出，执行器**无法自报成绩**。
